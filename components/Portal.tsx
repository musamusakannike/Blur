import { View, Text } from "react-native";

export const Portal = () => {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Portal</Text>
    </View>
  );
};
